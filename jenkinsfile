pipeline {
      agent any 
      stages {
          stage('Upload TO AWS') {
              steps   {
                   sh 'echo "Hello World"'
                   sh '''
                   echo "Multineline shell steps works too"
                   ls -lah
                   '''
                   }
                  }
                 }
                }
