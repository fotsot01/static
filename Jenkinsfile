pipeline {
      agent any 
      stages {
          stage('Upload TO manger danger AWS') {
              steps   {
                   sh 'echo "Hello World"'
                   sh '''
                   echo "Multineline shell steps works too"
                   ls -lah
                   '''
                   }
                  }
                 }
                }
